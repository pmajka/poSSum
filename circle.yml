dependencies:
  pre:
    - sudo apt-get update;
    - sudo apt-get update sudo apt-get install build-essential python-dev python-numpy python-setuptools imagemagick python-scipy build-essential cmake-curses-gui imagemagick parallel;
